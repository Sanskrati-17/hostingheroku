{
  "name": "scheduler",
  "version": "0.0.0",
  "server": false,
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "serve": "turbo devserver",
    "test": "",
    "build": "",
    "heroku-postbuild": "npm run build",
    "postinstall": "ng build --aot"
  },
  "engines": {
        "npm": "5.6.0",
        "node": "8.10.0"
      },
  "dependencies": {
    "cli": "^1.0.1",
    "express": "^4.17.1",
    "heroku": "^7.54.1",
    "mysql2": "^2.2.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.2",
    "rimraf": "^2.6.2"
  },
  "format": "vertex"
}
